import React, { useEffect, useState } from "react";
import { bd, collVideos } from "../code/init";
import "./AfficherVideos.scss";

export default function AfficherVideos({ utilisateur }) {
  const [videos, setVideos] = useState([]);
}
